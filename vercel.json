{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @greenfield/web build",
  "outputDirectory": ".next",
  "crons": [
    { "path": "/api/cron/supabase-keepalive", "schedule": "0 12 * * 0" }
  ]
}
