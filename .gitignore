# Dependencies
node_modules
.pnpm-store

# Builds
.next
out
dist
build
.turbo
*.tsbuildinfo

# Env
.env
.env.*
!.env.example
.env.local
.env.*.local

# Vercel
.vercel

# IDE / OS
.DS_Store
.vscode
.idea

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test coverage
coverage

# Next.js
next-env.d.ts

# Drizzle — generated artifacts. The .sql migration files themselves are tracked
# (PRs diff schema changes via SQL diffs); meta/ and snapshot/ are build artifacts.
# Drizzle-kit places these inside packages/db/src/migrations/ (alongside the .sql).
packages/db/src/migrations/meta
packages/db/src/migrations/*.json

# pnpm
.pnpm-debug.log*
