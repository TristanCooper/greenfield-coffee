// @greenfield/db
//
// Placeholder. Drizzle schema, Supabase client wiring, and the multi-tenant
// query helpers land in card 0.3 (Supabase) + 0.4 (Drizzle schema).

export const PACKAGE_NAME = '@greenfield/db' as const;
